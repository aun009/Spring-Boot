spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo_db
    username: root
    password: Your_PASS
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true